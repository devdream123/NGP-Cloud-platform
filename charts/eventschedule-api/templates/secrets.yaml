{{- if ne .Values.database.auth "iam" }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "eventschedule-api.fullname" . }}-secrets
  labels:
    {{- include "eventschedule-api.labels" . | nindent 4 }}
data:
  db-password: {{ .Values.database.pwd | b64enc }}
{{- end }}

