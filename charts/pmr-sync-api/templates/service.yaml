{{- if .Values.shouldDeploy }}
apiVersion: v1
kind: Service
metadata:
  name: {{ include "pmr-sync-api.fullname" . }}
  labels:
    {{- include "pmr-sync-api.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "pmr-sync-api.selectorLabels" . | nindent 4 }}
{{- end }}