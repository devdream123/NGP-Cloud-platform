{{- if .Values.shouldDeploy }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "pmr-sync-api.fullname" . }}-secrets
  labels:
    {{- include "pmr-sync-api.labels" . | nindent 4 }}
data:
  PMR_ENDPOINT_API_CREDENTIAL: {{ .Values.secrets.pmrEndpointAPICredential | b64enc }}
{{- end }}