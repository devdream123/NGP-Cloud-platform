replicaCount: 3

backendNamespace: ngp-backend 

dealSheet:
  chartName: dealsheet-api
  port: "80"

calendar: 
  chartName: calendar-api
  port: "80"

eventSchedule: 
  chartName: eventschedule-api
  port: "80"

hierarchy: 
  chartName: hierarchy-api
  port: "80"

mesh:
  cors:
    origin: '*'

  debugEnabled: 1

  fullname: graphql-mesh
  
  labels:
    run: graphql-mesh

  selectorLabels:
    run: graphql-mesh

server:
  telemetryHost: otel-collector.ngp-graphql-mesh.svc.cluster.local
  telemetryPort: 6832

image:
  repository: "us-central1-docker.pkg.dev/gcp-wow-corp-infra-qrtl-prod/hub-prd-us1-backend-services-registry1/qr-ngp-graphql-mesh"
  pullPolicy: Always
  tag: ""

imagePullSecrets: []
nameOverride: ""
fullnameOverride: ""

serviceAccount:
  create: false
  annotations: {}
  name: ""

podAnnotations: {}

podSecurityContext: {}

securityContext: {}

service:
  type: ClusterIP
  port: 80

livelinessProbe:
  path: /healthcheck
  port: http
  initialDelaySeconds: 300
  periodSeconds: 120

readinessProbe:
  path: /readiness
  port: http
  initialDelaySeconds: 300
  periodSeconds: 120

resources:
  requests:
    memory: 100Mi
    cpu: 100m
  limits:
    memory: 2Gi
    cpu: 2000m

nodeSelector: {}

tolerations: []

affinity: {}

PodDisruptionBudget:
  minAvailable: 66% #K8s rounds up to the nearest integer if the Replica Count is odd