{{- if ne .Values.database.auth "iam" }}
apiVersion: v1
kind: Secret
type: Opaque
metadata:
  name: {{ include "forecast-api.fullname" . }}-secrets
  labels:
    {{- include "forecast-api.labels" . | nindent 4 }}
data:
  QF_POSTGRES_PASSWORD: {{ .Values.database.pwd | b64enc }}
{{- end }}
