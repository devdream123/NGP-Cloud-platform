apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ include "calendar-api.fullname" . }}
  labels:
    {{- include "calendar-api.labels" . | nindent 4 }}
spec:
  replicas: {{ .Values.replicaCount }}
  selector:
    matchLabels:
      {{- include "calendar-api.selectorLabels" . | nindent 6 }}
  template:
    metadata:
      annotations:
      {{- with .Values.podAnnotations }}
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- include "calendar-api.configChecksum" . | nindent 8 }}
      labels:
        {{- include "calendar-api.selectorLabels" . | nindent 8 }}
    spec:
      {{- with .Values.imagePullSecrets }}
      imagePullSecrets:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      securityContext:
        {{- toYaml .Values.podSecurityContext | nindent 8 }}
      containers:
        - name: {{ .Chart.Name }}
          securityContext:
            {{- toYaml .Values.securityContext | nindent 12 }}
          image: "{{ .Values.image.repository }}:{{ .Values.image.tag | default .Chart.AppVersion }}"
          imagePullPolicy: {{ .Values.image.pullPolicy }}
          envFrom:
            - configMapRef:
                name: {{ include "calendar-api.fullname" . }}-cm
            - secretRef:
                name: {{ include "calendar-api.fullname" . }}-secrets
          env:
            - name: ConnectionStrings__SlottingDb
              value: "{{ printf "Server=%s;Port=%s;Database=%s;UID=%s;PWD=" .Values.database.server .Values.database.slotting.proxyPort .Values.database.slotting.dbName .Values.database.slotting.user }}"
            - name: ConnectionStrings__PlaceholderDb
              value: "{{ printf "Server=%s;Port=%s;Database=%s;UID=%s;PWD=" .Values.database.server .Values.database.placeholder.proxyPort .Values.database.placeholder.dbName .Values.database.placeholder.user }}"
            - name: SPLUNK_OTEL_AGENT
              valueFrom:
                fieldRef:
                  fieldPath: status.hostIP
            - name: OTEL_EXPORTER_OTLP_ENDPOINT
              value: "http://$(SPLUNK_OTEL_AGENT):4318"
          ports:
            - name: http
              containerPort: {{ .Values.service.port }}
              protocol: TCP
          livenessProbe:
            httpGet:
              path: {{ .Values.livelinessProbe.path }}
              port: {{ .Values.livelinessProbe.port }}
            initialDelaySeconds: {{ .Values.livelinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.livelinessProbe.periodSeconds }}
            failureThreshold: {{ .Values.livelinessProbe.failureThreshold }}
          readinessProbe:
            httpGet:
              path: {{ .Values.readinessProbe.path }}
              port: {{ .Values.readinessProbe.port }}
            initialDelaySeconds: {{ .Values.readinessProbe.initialDelaySeconds }}
            periodSeconds: {{ .Values.readinessProbe.periodSeconds }}
          resources:
            {{- toYaml .Values.resources | nindent 12 }}
        {{- if eq .Values.database.mode "postgres" }}
        - name: cloud-sql-proxy-slotting
          image: "gcr.io/cloud-sql-connectors/cloud-sql-proxy:{{ .Values.database.proxyVersion }}"
            {{- if eq .Values.database.auth "iam" }}
          command: [
            "/cloud-sql-proxy",
            "--auto-iam-authn",
            "--private-ip",
            "--port={{ .Values.database.slotting.proxyPort }}",
            "--impersonate-service-account={{ .Values.database.slotting.user }}.gserviceaccount.com",
            "{{ .Values.projectId }}:{{ .Values.database.region }}:{{ .Values.database.instance }}"
          ]
            {{- else }}
          command: [
            "/cloud-sql-proxy",
            "--private-ip",
            "{{ .Values.projectId }}:{{ .Values.database.region }}:{{ .Values.database.instance }}"
          ]
            {{- end }}
          securityContext:
            runAsNonRoot: true
          resources:
            {{- toYaml .Values.database.proxyResources | nindent 12 }}
        - name: cloud-sql-proxy-placeholder
          image: "gcr.io/cloud-sql-connectors/cloud-sql-proxy:{{ .Values.database.proxyVersion }}"
            {{- if eq .Values.database.auth "iam" }}
          command: [
            "/cloud-sql-proxy",
            "--auto-iam-authn",
            "--private-ip",
            "--port={{ .Values.database.placeholder.proxyPort }}",
            "--impersonate-service-account={{ .Values.database.placeholder.user }}.gserviceaccount.com",
            "{{ .Values.projectId }}:{{ .Values.database.region }}:{{ .Values.database.instance }}"
          ]
            {{- else }}
          command: [
            "/cloud-sql-proxy",
            "--private-ip",
            "{{ .Values.projectId }}:{{ .Values.database.region }}:{{ .Values.database.instance }}"
          ]
          securityContext:
            runAsNonRoot: true
          resources:
            {{- toYaml .Values.database.proxyResources | nindent 12 }}
            {{- end }}
        {{- end }}
      {{- with .Values.nodeSelector }}
      nodeSelector:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.affinity }}
      affinity:
        {{- toYaml . | nindent 8 }}
      {{- end }}
      {{- with .Values.tolerations }}
      tolerations:
        {{- toYaml . | nindent 8 }}
      {{- end }}
