projectId: gcp-wow-corp-qretail-ngp-tst

image:
  tag: v1.43.2-dev

firestore:
  calendarSettings:
    minimumBatchSize: 400
    maximumDegreeOfParallelism: 1
  dealsheetSettings:
    minimumBatchSize: 400
    maximumDegreeOfParallelism: 1

database:
  mode: postgres
  instance: ngp-tst-us1-services1-sql
  user: postgres
  pwd: 

secrets:
  typesenseAPIKey:
