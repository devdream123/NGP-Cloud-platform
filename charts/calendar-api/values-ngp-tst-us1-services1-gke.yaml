image:
  repository: us-central1-docker.pkg.dev/gcp-wow-corp-infra-qrtl-prod/hub-prd-us1-backend-services-registry1/qr-ngp-calendar
  tag: v2.0.1-dev

firestoreSettings:
  rootCollectionName: "calendar-assortments_flexi"

serviceSettings:
  calendar:
    firestoreSettings:
      rootCollectionName: "calendar-assortments_flexi"
  dealsheet:
    firestoreSettings:
      rootCollectionName: "dealsheet-assortments_flexi"
  scenario:
    firestoreSettings:
      rootCollectionName: "scenarios_flexi"
  placeholder:
    firestoreSettings:
      rootCollectionName: "placeholders_flexi"