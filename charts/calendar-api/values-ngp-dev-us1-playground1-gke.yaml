projectId: gcp-wow-corp-qretail-ngp-dev

image:
  tag: v0.0.0415-dev

firestore:
  calendarCollection: calendar-assortments-pg
  calendarSettings:
    minimumBatchSize: 400
    maximumDegreeOfParallelism: 1
  dealsheetCollection: dealsheet-assortments-pg
  dealsheetSettings:
    minimumBatchSize: 400
    maximumDegreeOfParallelism: 1
  scenariosCollection: scenarios-pg
  placeholdersCollection: placeholders-pg

database:
  mode: postgres
  instance: ngp-dev-us1-services1-sql
  user: postgres
  pwd: 

secrets:
  typesenseAPIKey: