# https://yamllint.readthedocs.io/en/stable/configuration.html

extends: default

rules:
  line-length: disable
  document-start: disable
  braces: disable
  truthy: disable

ignore:
  - charts/*/templates/
