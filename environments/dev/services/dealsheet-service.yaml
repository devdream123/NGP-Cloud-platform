---
apiVersion: v1
kind: Service
metadata:
  name: dealsheet-api
  namespace: ngp
  labels:
    run: dealsheet-api
spec:
  type: ClusterIP
  ports:
    - port: 8080
      targetPort: 8080
      protocol: TCP
  selector:
    app: dealsheet-api
